# Server Configuration
PORT=8000

# RAG Service API Key (if your RAG API is protected)
RAG_API_KEY="your_rag_api_key_here"

# ChromaDB Connection URL
CHROMA_URL="http://localhost:8001"

# OpenAI Configuration
OPENAI_API_KEY="your_openai_api_key_here"
# Optional: Specify a default OpenAI embedding model
# OPENAI_EMBEDDING_MODEL="text-embedding-ada-002"
# Optional: Specify a default OpenAI chat model
# OPENAI_CHAT_MODEL="gpt-3.5-turbo"

# Gemini Configuration
GEMINI_API_KEY="your_gemini_api_key_here"
# Optional: Specify a default Gemini embedding model
# GEMINI_EMBEDDING_MODEL="embedding-001"
# Optional: Specify a default Gemini chat model
# GEMINI_CHAT_MODEL="gemini-pro"

# Base URLs (optional, defaults are usually fine)
# OPENAI_BASE_URL="https://api.openai.com/v1"
# GEMINI_BASE_URL="https://generativelanguage.googleapis.com/v1beta"

# ChromaDB Specific Configuration (if applicable)
CHROMA_TENANT="default_tenant" # Optional: specify tenant if not using default
CHROMA_DATABASE="default_database" # Optional: specify database if not using default
CHROMA_AUTH_TOKEN="" # Optional: specify auth token if ChromaDB requires authentication
# CHROMA_USERNAME="" # Alternative: if using basic auth
# CHROMA_PASSWORD="" # Alternative: if using basic auth
